---
tasks:
    - name: Clean up redundant / incomplete symlinks

    - name: Remove unlinked config files

    - name: Remove unlinked .config directories
      loop:
        - .config/nvim/
        - .config/wezterm/
        - .config/zsh/
        - .config/lazygit/

    - name: Stow dotfiles
      shell: stow {{ item }}
      loop:
        - stow
        - nvim
        - bash
        - zsh
        - wezterm
        - git
        - vim
        - lazygit
        - starship
        - linters
        - zk
        - bin

    - name: Copy local_env_vars template

    - name: Configure global gitignore
